# ============================================================
#  CDC 云端接入配置模板
#  用法：复制本文件为 cdc_srv.dat，放在 cdc_backend.exe 同目录
#  （文件名必须正好是 cdc_srv.dat；旧名 cdc_cloud.cfg 仍被兼容读取）
# ============================================================
#
#  server = 云端接口的基础地址（形如 https://主机[:端口][/基础路径]）
#  末尾斜杠会被自动去掉；只认键名 server 或 s。
#
#  例：
#    server=https://api.example.com/cdc_server
#    server=http://127.0.0.1:8080
#
server=
#
# ------------------------------------------------------------
#  可选：十六进制 XOR 混淆写法（键 x:）
#  把地址每个字节与键 "cdc_7_1_xor" 循环异或，再写成十六进制，
#  这样配置被翻到也看不出真实地址。
#  与 cdc_backend.cpp 中 kCloudEnc 使用的是同一把键。
#
#  x:0A1B2C3D4E...
#
# ------------------------------------------------------------
#  说明：
#   1. 本文件优先级高于编译进 exe 的内置地址。
#   2. 不配置也可以——云端相关接口会返回 cloud_not_configured，
#      本地生成数据等功能完全不受影响。
#   3. 以 # 开头的行为注释；键名两侧空白会被忽略。
#   4. 本文件含服务器地址，请勿提交到公开仓库（已在 .gitignore 中排除）。
# ============================================================
